## 任务概述

- 目标：对 `shipany-template-two` 示例库进行完整架构分析，并与主仓 `mksaas_template-main` 的架构规范进行对比，提炼可推广的精华设计与需要改进的问题，为后续模板统一化与演进提供依据。
- 范围：
  - 重点分析 `shipany-template-two` 的目录结构、分层、模块边界、数据流/请求流以及可扩展性/可维护性。
  - 结合主仓的架构文档与现有实现，给出对比结论和目标架构蓝图建议。

## 执行计划（方案 2）

1. 收集架构文档与约束  
   - 阅读主仓的 `AGENTS.md`、`README.md`、`docs/architecture-overview.md`、`docs/feature-modules.md`、`docs/governance-index.md` 等核心文档，提取架构分层、模块边界、错误处理、环境/部署等约束，作为对比标尺。

2. 分析 `shipany-template-two` 目录结构与关键文件  
   - 浏览 `shipany-template-two` 目录结构，识别入口、路由/页面、服务层、配置/环境、基础设施封装等关键模块。
   - 对代表性文件进行抽样阅读，理解其职责与依赖关系。

3. 梳理 `shipany-template-two` 架构视图（A/B/C/D）  
   - A：高层架构概览（技术栈、运行时形态、分层结构）。  
   - B：目录结构 + 模块职责说明。  
   - C：典型数据流 / 请求流（从 UI/路由 → 业务逻辑 → 外部依赖/存储）。  
   - D：可扩展性/可维护性评估（耦合点、隐式依赖、可抽象/可下沉部分）。

4. 整理主仓 `mksaas-template-main` 架构概览（缩略画像）  
   - 使用第 3 步相同视角，对主仓做一个对照用的缩略架构画像。

5. 对比两者并提炼精华与问题  
   - 提炼 `shipany-template-two` 在架构和实现上的亮点（可推广为统一模板规范）。  
   - 识别与主仓标准不一致或质量偏弱的部分，并初步划分短期可修与长期演进项。

6. 给出目标架构蓝图与演进建议  
   - 基于主仓标准与 `shipany-template-two` 优点，提出目标模板架构蓝图（分层、目录、数据流模式）。  
   - 明确如果要将 `shipany-template-two` 打造成推荐示例库，需要的调整清单与优先级。

## 预期输出

- `shipany-template-two` 架构分析报告：包含 A/B/C/D 四个视角。
- 主仓 `mksaas-template-main` 缩略架构画像。
- 二者对比下的「精华列表」与「问题列表」。
- 一份面向未来模板的目标架构蓝图与具体演进建议。

