# 文档审查任务

## 上下文
- 目标：审查 `docs/`、`README.md`、`AGENTS.md`、`CLAUDE.md` 的架构/API 文档是否完整且无明显错漏。
- 关注点：体系结构描述、API 约定、运维/错误模型同步情况，以及是否与 Diátaxis 四象限相匹配。

## 执行计划
1. 收集资料：逐一阅读 `docs/` 下的核心文件及根目录说明文档，整理架构与 API 相关内容节点。
2. 建立覆盖矩阵：以主要主题 vs. 文档来源的方式对照信息覆盖，并结合 Diátaxis 判定各文档类型与缺漏。
3. 输出评估：根据矩阵与阅读笔记撰写评估报告，仅列出发现、风险与建议，不直接修改文档。
